import {first, second} from './main';

//import {first as one} from './main';

//import * as data from './main';
//console.log(`${data.first} and ${data.second}`);

console.log(`${first} and ${second}`);